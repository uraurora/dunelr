package enums;

/**
 * 发送方是属于source还是target，为了确认是client是想发起同步还是需要同步
 *
 */
public enum DirectionEnum {
    /**
     * 发送方式source
     */
    SOURCE,

    /**
     * 发送方是target
     */
    TARGET,
    ;

}
